# Prompt Log — Лабораторная работа №12

## Задание 1 — Создание полноценного веб-приложения (повышенная сложность)

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Ты — senior Python разработчик. Создай полноценное веб-приложение для предметной области 'CRM для отдела продаж'. Используй FastAPI, SQLAlchemy, Pydantic v2, SQLite. Сущности: Client (основная), Contact, Deal, Task, User. Реализуй: JWT-аутентификацию (регистрация/логин), полный CRUD для каждой сущности, ролевую модель (admin/manager/viewer), воронку продаж с автоматическим обновлением вероятности, аналитические эндпоинты. Структурируй код по слоям: models, schemas, api, core."  
**Результат:** Получила полностью рабочее FastAPI-приложение с 33 файлами: модели SQLAlchemy, Pydantic-схемы, роутеры, JWT-аутентификация, аналитика с SQL-агрегацией, seed-скрипт с демо-данными. Код запустился практически без правок.

### Промпт 2
**Промпт:** "Сгенерируй подробную документацию проекта в формате Markdown. Опиши каждый файл, его назначение, API-эндпоинты в виде таблиц, инструкцию по запуску для Windows/Linux, примеры curl-запросов, описание ролевой модели и переменных окружения."  
**Результат:** Получила файл CRM_Documentation.md с полным описанием всех модулей, таблицами эндпоинтов, примерами запросов и инструкцией по настройке окружения.

### Промпт 3
**Промпт:** "При запуске приложения получаю ошибку: pydantic.errors.PydanticUserError: A non-annotated attribute was detected: ContactOut. Файл app/schemas/client.py, класс ClientDetail. Как исправить?"  
**Результат:** Получила объяснение причины (импорты внутри тела класса Pydantic воспринимаются как поля) и исправленный файл с использованием TYPE_CHECKING и from __future__ import annotations.

### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: перенос импортов в schemas/client.py, активация venv на Windows
- Время: 40 мин

---

## Задание 2 — Code review сгенерированного кода (повышенная сложность)

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Проведи критический анализ следующего Python-кода FastAPI приложения. Найди не менее 5 проблем из категорий: логические ошибки, проблемы производительности, нарушения стиля PEP8, устаревший API, мёртвый код, отсутствие обработки исключений. Для каждой проблемы укажи файл, описание и исправленный вариант. Оформи отчёт в формате: Что сгенерировал ИИ → В чём проблема → Как исправил."  
**Результат:** Получила детальный анализ с 6 найденными проблемами: неиспользуемые enum-классы, устаревший импорт declarative_base, ленивые импорты внутри функций, логический баг с дефолтным значением probability=10, устаревший синтаксис class Config в Pydantic v2, избыточные конструкции в коде.

### Промпт 2
**Промпт:** "В аналитическом модуле analytics.py функция get_summary загружает все сделки и задачи в память Python через .all() и фильтрует их циклами. При большом объёме данных это критично. Перепиши с использованием SQL-агрегации через func.count, func.sum и case(). Также устрани N+1 запрос в manager_performance."  
**Результат:** Получила оптимизированную версию аналитики: вместо загрузки всех строк в память теперь один SQL-запрос с агрегацией возвращает готовые числа. manager_performance переписан с одним GROUP BY запросом вместо цикла по каждому менеджеру.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: выбрала 5 исправлений из 6 предложенных для включения в финальный отчёт
- Время: 20 мин

---

## Задание 4 — Интеграция ИИ в CI/CD (повышенная сложность)

### Промпт 1
**Инструмент:** Claude  
**Промпт:** "Настрой GitHub Actions workflow для автоматического AI-ревью кода при создании Pull Request. Требования: триггер на события opened и synchronize, получение git diff изменённого кода, отправка diff во внешний LLM API, публикация ревью комментарием в PR через GitHub API. Используй бесплатный LLM API без необходимости привязки карты. Ревью должно быть на русском языке и содержать секции: что хорошо, замечания, баги, предложения по улучшению."  
**Результат:** Получила готовый workflow на основе Groq API с моделью llama-3.3-70b-versatile. Включает шаги: checkout, получение diff, Python-скрипт для вызова Groq API, публикация результата через actions/github-script. Ревью публикуется с эмодзи и ссылкой на лог Actions.

### Промпт 2
**Промпт:** "Объясни пошагово: как получить API-ключ Groq, как безопасно хранить секреты в GitHub репозитории, как создать тестовую ветку и Pull Request для проверки работы workflow."  
**Результат:** Получила пошаговую инструкцию с указанием конкретных действий в интерфейсе GitHub. Описан полный цикл от регистрации на console.groq.com до появления комментария в PR.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего, workflow заработал с первого раза
- Время: 30 мин

---

## Задание 7 — Генерация unit-тестов с высоким покрытием (повышенная сложность)

### Промпт 1 — Базовая структура тестов
**Инструмент:** Claude  
**Промпт:** "Разработай систему pytest-тестов для FastAPI CRM приложения с покрытием не менее 90%. Приложение имеет сущности User, Client, Contact, Deal, Task и ролевую модель admin/manager/viewer. Для каждого эндпоинта реализуй: happy path, негативные сценарии (404, 400), проверку прав доступа (403, 401), граничные случаи. Используй TestClient, SQLite in-memory БД, фикстуры с автоматической очисткой БД между тестами. Вынеси вспомогательные функции register/login/make_client/make_deal в хелперы."  
**Результат:** Получила архитектуру тестового файла с 7 классами и вспомогательными функциями. Базовый скелет с happy path для каждого из 30+ эндпоинтов.

### Промпт 2 — Граничные случаи предметной области
**Промпт:** "Дополни тесты граничными случаями специфичными для CRM-домена: сделка с amount=0 и amount=999999999, автоматическое проставление actual_close_date при переводе в closed_won/closed_lost, автоматическое обновление probability при смене стадии, задача с due_date в прошлом (не должна вызывать ошибку), задача без привязки к клиенту и сделке, фильтр overdue_only=true, менеджер видит только своих клиентов в списке."  
**Результат:** Получила 18 дополнительных тестов граничных случаев. Зафиксировано корректное поведение: нулевая сумма сделки принимается системой без ошибки.

### Промпт 3 — Тесты аналитики с защитой от деления на ноль
**Промпт:** "Сгенерируй тесты для аналитических эндпоинтов /analytics/*. Обязательно проверь: summary возвращает нули при пустой БД и не падает с ZeroDivisionError, win_rate корректен при отсутствии закрытых сделок, pipeline-funnel всегда содержит все 6 стадий даже при пустой БД, deals-by-month принимает граничные значения months=1 и months=24, admin видит статистику всех менеджеров а manager только свою."  
**Результат:** Получила 11 тестов аналитики. Тест на деление на ноль при нулевом win_rate критически важен — без него баг легко пропустить в production.

### Промпт 4 — Тесты безопасности и JWT
**Промпт:** "Сгенерируй тесты для проверки системы аутентификации и авторизации: запрос без Bearer токена должен вернуть 403, запрос с невалидной подписью JWT — 401, первый зарегистрированный пользователь получает роль admin, повторная регистрация с тем же email — 400, viewer не может выполнять мутирующие операции (POST/PUT/DELETE) — 403, admin не может удалить собственный аккаунт — 400."  
**Результат:** Получила 11 тестов безопасности, покрывающих все критичные сценарии аутентификации и ролевой модели.

### Итого
- Количество промптов: 4
- Итоговое количество тестов: 89
- Покрытие: ≥ 90%
- Что пришлось исправлять вручную: порядок регистрации пользователей в тесте test_delete_client_requires_admin — первый зарегистрированный становится admin, поэтому порядок вызовов имеет значение
- Время: 35 мин
